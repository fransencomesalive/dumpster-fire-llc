import PlanClient from "./PlanClient";

export const metadata = {
  title: "Choose your plan",
  description: "Pick a plan or redeem an access code to start building your profile.",
};

export default function PlanPage() {
  return <PlanClient />;
}
