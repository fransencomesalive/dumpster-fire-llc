import SignUpClient from "./SignUpClient";

export const metadata = {
  title: "Create your profile",
  description: "Create your account to build the profile behind your outreach.",
};

export default function SignUpPage() {
  return <SignUpClient />;
}
